name: Non-Functional CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  nonfunctional-checks:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install quality tools
        run: |
          python -m pip install --upgrade pip
          pip install ruff bandit

      - name: Run critical Ruff checks
        run: |
          ruff check src \
            --select E9,F63,F7,F82 \
            --exclude "src/uav_risk/stage2/knowledge/models/**"

      - name: Generate full Ruff report
        continue-on-error: true
        run: |
          ruff check src \
            --exclude "src/uav_risk/stage2/knowledge/models/**" \
            --output-format=github

      - name: Run Bandit security scan
        continue-on-error: true
        run: |
          bandit -r src -ll -x src/uav_risk/stage2/knowledge/models
